def retrieve_context(user_request: str) -> str:
    return (
        "Retrieved context placeholder:\n"
        "LangGraph is useful for building stateful AI workflows with multiple steps.\n"
        "LangChain is useful for building LLM applications with prompts, chains, tools, and integrations."
    )